# WAF node configuration files

DEB and RPM packages:

- /etc/nginx/conf.d/default.conf with NGINX settings
- /etc/nginx/conf.d/wallarm.conf with global WAF node settings
- ...

Docker images:

- /etc/nginx/sites-enabled/default